name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
      - name: Build static site
        env:
          # Production Supabase project (Settings → API). Optional until wired.
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY }}
          # Private surface unlock codes (hashed into the client at build).
          # Repo → Settings → Secrets and variables → Actions
          # UNIVERSAL_ACCESS_CODE unlocks docs + all product surfaces.
          UNIVERSAL_ACCESS_CODE: ${{ secrets.UNIVERSAL_ACCESS_CODE }}
          # Optional docs-only code (still supported alongside universal).
          DOCS_ACCESS_CODE: ${{ secrets.DOCS_ACCESS_CODE }}
          # Optional product-only code.
          PRODUCT_ACCESS_CODE: ${{ secrets.PRODUCT_ACCESS_CODE }}
        run: npm run build:pages
      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
